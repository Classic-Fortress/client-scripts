// please tweak df and ds settings below to your default fov (df) and default sensitivity (ds)
// the zoom function depends on these settings being set correctly
// they default to 90 (df) and 3 (ds)
setinfo df 90 // default fov
setinfo ds 3 // default sensitivity
alias f_spawn "set_sbar; setinfo df 90; setinfo ds 3" // don't forget to edit df and ds here too!

// config start
unbindall
+mlook

// movement bindings
bind w "+forward"
bind s "+back"
bind a "+moveleft" // strafe left
bind d "+moveright" // strafe right
bind space "+jump"
bind ctrl "+movedown" // swim down

// communication bindings
bind t "messagemode" // type message to all
bind y "messagemode2" // type message to team only
bind c "saveme" // call for medic

// gameplay bindings
bind , "changeclass" // change class
bind . "changeteam" // change team
bind x "discard" // discard useless ammo
bind z "dropitems" // drop flag
bind e "special" // class special

// weapon handling bindings
bind mouse1 "+attack" // use weapon
bind mouse2 "+gren1" // throw hand grenade
bind mwheelup "weapprev" // prev weapon
bind mwheeldown "weapnext" // next weapon
bind shift "grenswitch" // swap grenade slots
bind q "weaplast" // last equipped weapon
bind f "+gren2" // throw special grenade
bind r "reload" // reload
bind 1 "impulse 1" // weapon slot 1 / primary weapon
bind 2 "impulse 2" // weapon slot 2 / secondary weapon
bind 3 "impulse 3" // weapon slot 3
bind 4 "impulse 4" // weapon slot 4 / melee weapon
bind 5 "impulse 5" // class menu
bind 6 "impulse 6"
bind 7 "impulse 7"
bind 8 "impulse 8"
bind 9 "impulse 9"
bind 0 "impulse 0"

// miscellaneous bindings
bind f5 "screenshot"
bind tab "+showteamscores"
bind escape "togglemenu"
bind ` "toggleconsole"

// video settings
gl_lightmode 2 // dark map with distinguishable fullbrights

// view settings
crosshair 1 // use default crosshair
scr_conspeed 9999 // make console toggle instantly
v_viewheight -6 // adjust view height to gun height, allowing more accurate aiming

// fps settings
cl_gibfilter 0 // do not remove gibs from map
cl_muzzleflash 1 // gunfire lights up area around player
gl_fb_bmodels 0 // do not make ammo/health models fullbright
gl_fb_models 0 // do not make player/ammo/projectile models fullbright
gl_flashblend 0 // light flashes light up world shadows
r_fullbrightskins 0 // do not allow fullbright skins

// gameplay settings
cl_fakeshaft 0 // do not adjust lightning bolt (required for functional scout scanner)
cl_forwardspeed 450 // maximal allowed forward speed
cl_sidespeed 450 // maximal allowed strafe speed
cl_backspeed 450 // maximal allowed backward speed
w_switch 2 // don't switch to new weapons
b_switch 2 // don't switch to new weapons (picked up from backpacks)
noskins 0 // allow skins
enemyskin "" // do not use a single skin for all enemies
teamskin "" // do not use a single skin for all teammates

// weapon script settings
cl_weaponhide 0 // do not hide current weapon when not used (useless in team fortress)
cl_weaponpreselect 0 // do not use preselected weapon (useless in team fortress)

// miscellaneous settings
rate 25000 // increase default transfer rate
demo_dir fortress/demos // save demos to this directory
sshot_dir fortress/screenshots // save screenshots to this directory
log_dir fortress/logs // save console logs to this directory
menu_ingame 0 // turn off ingame menu (useless in team fortress)

// alias for setting the sbar correctly according to resolution
set sb_offset 0 // tweak this number to change sbar location
alias set_sbar "set_calc sb_lines $win_height / $vid_conscale; set_calc sb_lines $sb_lines / 8; set_calc sb_lines $sb_lines - 12; set_calc sb_lines $sb_lines + $sb_offset; setinfo sb $sb_lines"